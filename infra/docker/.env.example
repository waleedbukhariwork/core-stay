# LOCAL DEVELOPMENT ONLY.
# These credentials are disposable and must never be reused in dev, staging, or production.
POSTGRES_USER=codecore
POSTGRES_PASSWORD=codecore_local_only_not_for_prod
POSTGRES_DB=codecore
POSTGRES_PORT=55432
